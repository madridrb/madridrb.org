%article.type-system-sans
  %p.type User

  %h1
    = gravatar_tag user, size: 50
    = link_to user.name, user

  - if policy(user).edit?
    %p= link_to 'Edit', edit_user_path(user)

  = simple_format user.about

  %p
    = link_to_twitter user.twitter
    %br
    = link_to user.url, user.url
